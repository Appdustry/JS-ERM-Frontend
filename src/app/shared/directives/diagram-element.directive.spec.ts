import { DiagramElementDirective } from './diagram-element.directive';

describe('DiagramElementDirective', () => {
  it('should create an instance', () => {
    const directive = new DiagramElementDirective();
    expect(directive).toBeTruthy();
  });
});
